# Crowe Outcome Volume labels and logo fields

## Approved change

The five Three.js operating fields will be named directly in the animation rather than explained only in surrounding copy. Each field carries a restrained in-scene label: `01 Outcome`, `02 Workflow`, `03 Authority`, `04 Evidence`, and `05 Ownership`. The labels move with their fields, become fully legible as each field aligns, and remain visible in the settled composition. The WebGL fallback carries the same five names.

Every Crowe identity placement will sit on a deliberate white identity field. The header mark, hero lockup, and shared document logo receive consistent white backing, padding, and containment. The existing locally committed Crowe mark remains unchanged; this revision improves its presentation rather than tracing or recoloring the trademark.

## Motion and accessibility

The label reveal remains part of the existing one-shot alignment sequence. It does not add perpetual motion or pointer interaction. Reduced-motion users receive the fully labeled final state immediately. Canvas labels are duplicated in accessible stage text and in the visible fallback, so meaning is not dependent on WebGL.

## Verification

Source and browser gates must verify all five labels in the runtime and fallback, the settled scene, mobile containment, and white backing for `.brand-mini img`, `.company-lockup`, and `.doc-logo`.
