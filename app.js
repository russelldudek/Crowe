const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.site-nav');if(navToggle&&nav){navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}
const lab=document.querySelector('.lab-stage');const buttons=[...document.querySelectorAll('[data-scenario]')];const status=document.querySelector('#decision-status');const copy=document.querySelector('#decision-copy');
const states={baseline:{status:'ADVANCE — complete workstream',copy:'The outcome, workflow, authority, evidence, integration, and client owner are explicit. Build the smallest production-worthy slice and instrument adoption from day one.'},speed:{status:'REFRAME — fast is not yet complete',copy:'Executive urgency is valid, but delivery speed without authority, evidence, and ownership turns a visible prototype into a hidden operating liability.'},agent:{status:'BOUND — define the action envelope',copy:'When an agent can act across systems, permissions, traceability, exception handling, human authority, and fallback become part of the product—not review paperwork.'},orphan:{status:'HOLD — name the operating owner',copy:'A technically successful pilot without a client owner, cadence, and adoption mechanism is an isolated result. Ownership is a delivery requirement.'},legacy:{status:'SEQUENCE — prove the integration boundary',copy:'Where source systems or connectors are uncertain, test the boundary first. Platform selection should follow integration, control, and supportability evidence—not fashion.'}};
function setScenario(key){if(!lab||!states[key])return;lab.dataset.state=key;buttons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.scenario===key)));status.textContent=states[key].status;copy.textContent=states[key].copy;}
buttons.forEach(b=>b.addEventListener('click',()=>setScenario(b.dataset.scenario)));const reset=document.querySelector('#reset-scenario');if(reset)reset.addEventListener('click',()=>setScenario('baseline'));setScenario('baseline');
